{
  "name": "event-horizon-eat-the-universe",
  "version": "1.0.0",
  "description": "Event Horizon: Eat the Universe — a cosmic arcade/idle growth game for CrazyGames.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
